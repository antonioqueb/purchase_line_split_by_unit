<odoo>
    <!-- Vista LISTA (tree) -->
    <record id="stock_move_line_tree_view_inherit_sku" model="ir.ui.view">
        <field name="name">stock.move.line.tree.inherit.sku</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_move_line_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_id']" position="after">
                <field name="sku_unico" readonly="1"/>
                <field name="x_lote_comun" readonly="1"/>
                <field name="x_sku_prefix" readonly="1"/>
            </xpath>
        </field>
    </record>

    <!-- Vista FORMULARIO -->
    <record id="stock_move_line_form_view_inherit_sku" model="ir.ui.view">
        <field name="name">stock.move.line.form.inherit.sku</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_move_line_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='quantity']" position="after">
                <field name="sku_unico" readonly="1"/>
                <field name="x_lote_comun" readonly="1"/>
                <field name="x_sku_prefix" readonly="1"/>
            </xpath>
        </field>
    </record>

    <!-- Heredar formulario del picking: stock.view_picking_form -->
    <record id="stock_picking_form_inherit_lote_sku" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit.lote.sku</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <!-- Mostrar columna sku_unico y otros en el formulario del albarán -->
            <xpath expr="//field[@name='move_line_ids_without_package']/form//field[@name='product_id']" position="after">
                <field name="sku_unico" readonly="1"/>
                <field name="x_lote_comun" readonly="1"/>
                <field name="x_sku_prefix" readonly="1"/>
            </xpath>
        </field>
    </record>
</odoo>
